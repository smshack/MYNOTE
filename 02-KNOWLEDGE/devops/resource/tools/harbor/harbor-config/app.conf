[server]
http_addr = 0.0.0.0
http_port = 8080
# https를 직접 Core에서 처리하지 않고 Nginx를 통해 프록시할 경우 생략 가능

[database]
host = harbor-db
port = 5432
username = postgres
password = harbor123
database = registry
sslmode = disable

[cache]
type = redis
redis_host = redis
redis_port = 6379
redis_password =
